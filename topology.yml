# LEGACY COMPATIBILITY POINTER (non-canonical)
#
# This file intentionally does not contain an active topology definition.
# Canonical Subcase 1b topology source of truth:
#   sandboxes/topology_subcase_1b.yaml
#
# Do not use this file for KYPO imports, training packaging, or validation.
# Keep all topology changes in sandboxes/topology_subcase_1b.yaml.
legacy_pointer_to: sandboxes/topology_subcase_1b.yaml
status: deprecated
